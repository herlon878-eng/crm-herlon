<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>CRM Herlon</title>
    <style>
        body { font-family: Arial; margin: 40px; background: #f7f9fb; }
        h1 { color: #333; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { padding: 8px; border-bottom: 1px solid #ddd; text-align: left; }
        input { padding: 8px; margin: 5px; }
        button { padding: 8px 15px; background: #0a66c2; color: white; border: none; cursor: pointer; }
        button:hover { background: #004182; }
    </style>
</head>
<body>
    <h1>CRM - Clientes</h1>
    <form action="/adicionar" method="POST">
        <input type="text" name="nome" placeholder="Nome" required>
        <input type="email" name="email" placeholder="E-mail" required>
        <input type="text" name="telefone" placeholder="Telefone" required>
        <button type="submit">Adicionar</button>
    </form>

    <table>
        <tr><th>Nome</th><th>Email</th><th>Telefone</th><th>Ações</th></tr>
        {% for c in clientes %}
        <tr>
            <td>{{ c.nome }}</td>
            <td>{{ c.email }}</td>
            <td>{{ c.telefone }}</td>
            <td><a href="/excluir/{{ loop.index0 }}">Excluir</a></td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
